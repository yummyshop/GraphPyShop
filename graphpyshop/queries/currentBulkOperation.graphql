query currentBulkOperation($currentBulkOperation_type: BulkOperationType = QUERY) {
  currentBulkOperation(type: $currentBulkOperation_type) {
    completedAt
    createdAt
    errorCode
    fileSize
    id
    objectCount
    partialDataUrl
    query
    rootObjectCount
    status
    type
    url
  }
}