query deliveryPromiseProvider($locationId: ID!) {
  deliveryPromiseProvider(locationId: $locationId) {
    active
    fulfillmentDelay
    id
    location {
      id
      __typename
    }
    timeZone
    __typename
  }
}