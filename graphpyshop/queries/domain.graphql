query domain($domain_id: ID!) {
  domain(id: $domain_id) {
    host
    id
    localization {
      alternateLocales
      country
      defaultLocale
    }
    marketWebPresence {
      id
      subfolderSuffix
    }
    sslEnabled
    url
  }
}