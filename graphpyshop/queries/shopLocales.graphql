query shopLocales($shopLocales_published: Boolean = false) {
  shopLocales(published: $shopLocales_published) {
    locale
    marketWebPresences {
      id
      subfolderSuffix
    }
    name
    primary
    published
  }
}